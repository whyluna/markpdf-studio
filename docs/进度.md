# MarkPDF Studio 开发进度

> 与《需求文档 v1.0》（FR-x.x 编号）逐项对应。
> **更新约定**：每完成/变更一项即更新本文件并随代码同提交（开发规范 §5）。

| 图例 | 含义 |
|---|---|
| ✅ | 完成（含真机验证） |
| 🚧 | 进行中 / 部分完成 |
| ⬜ | 未开始 |
| ⏸️ | 延后（按 PRD 排期在后续里程碑） |

## 里程碑总览

| 里程碑 | 内容 | 状态 |
|---|---|---|
| M1 · MVP | 模块 1 全部 P0、FR-2.1/2.2/2.3/2.6/2.7、FR-3.1~3.4、FR-6.1、FR-7.1 | ✅ 已验收（2026-07-18） |
| M2 · 标注 | 模块 4 全部 P0、FR-4.3、FR-5.1 | ✅ 已验收（2026-07-19；FR-4.2 便签经决策移除） |
| M3 · 联动与完善 | 剩余 P1 | ✅ 已验收（2026-07-20） |
| M4 · 打磨 | P2、性能、签名分发 | ✅ 功能完成；性能例检通过（2026-07-21）；0.1.0/0.1.1 未签名 DMG + GitHub Release 分发（决策：不购 Developer ID） |

---

## 模块 1：文件与项目管理

| 编号 | 需求 | 状态 | 提交 / 备注 |
|---|---|---|---|
| FR-1.1 | 工作区文件树 | ✅ | `8bd6262`；后续交互修复（整行点击/新建文件夹可见） |
| FR-1.2 | 文件树操作（新建/重命名/删除/拖拽/右键/Cmd+Z） | ✅ | `65dd8c7`、`34287cb`（改名交互/打开中文件跟随） |
| FR-1.3 | FSEvents 外部变更自动刷新 | ✅ | `f5f0e97` |
| FR-1.4 | 标签页 + 左右分栏 | ✅ | `62a1a3e`、`8f7d822`；限制：标签切换内核撤销栈重置 |
| FR-1.5 | 收藏夹、最近打开 | ✅ | `FavoritesStore`/`RecentFilesStore`（按工作区根路径存 UserDefaults；最近打开去重置顶，**上限 10 条（用户决策，PRD 原 20），读取时自动清理已删除文件并回写**）；文件树新增「收藏/最近打开」分区（对齐设计稿 .sec）；右键「加入收藏」；`TabStore.onOpenFile` 钩子集中记录；FileNode.Kind 下图标属性；9 单测；真机验收通过（2026-07-20） |
| FR-1.6 | 工作区状态持久化 | ✅ | `WorkspaceStateStore`：快照 JSON 存 UserDefaults（0.5s 防抖 + 退出 flush）；根目录存 security-scoped bookmark（新增 app-scope entitlement，恢复后保持访问权）；标签组/分栏/激活状态重建（空组丢弃、全空保留草稿）；md 光标行经内核 `editor.cursor` 上报 + setContent `initialLine` 恢复；PDF 位置由 FR-3.5 覆盖；Debouncer.fire 修复（先 cancel 后 perform 为空操作的 bug，附回归测试）；6+2 单测；真机验收通过（2026-07-20）。**增强（2026-07-22）：标签现场按工作区隔离分槽**——切换工作区时旧现场入槽、新工作区恢复自己的标签（此前换工作区旧标签不请自来）；`TabStore.replaceAll` 支持多次整体替换（启动 restore 仍一次性守卫）；v1 快照自动迁移入槽；槽位 key 统一标准化防 /tmp 符号链接分叉；重开当前工作区 no-op。**修复切换清空标签**：切换编排期间 root 仍是旧工作区（异步扫描未完成），collapsedFolders 赋值等路过事件以旧 root 触发 `workspaceDidChange` 把槽位指针拉回旧槽，随后 replaceAll 把空白草稿写进旧槽覆盖真实标签（实机复现：三个工作区槽位全被洗成草稿）→ `pendingSwitchPath` 挡旧 root 事件，新 root 到达才落定；全接线回归测试锁定。**偏差：侧栏宽度与分栏比例不持久**（NavigationSplitView/HSplitView 在 macOS 13 无宽度观测 API）；**草稿内容不持久**（恢复为空草稿） |

## 模块 2：Markdown 编辑与阅读

| 编号 | 需求 | 状态 | 提交 / 备注 |
|---|---|---|---|
| FR-2.1 | 所见即所得编辑 | ✅ | 光标映射修复（CM6 行高测量 margin 漂移）、drawSelection 自绘光标 |
| FR-2.2 | 三模式切换 | ✅ | 所见即所得 / 源码 / 阅读 |
| FR-2.3 | GFM 全集 | ✅ | `203d406` 表格渲染+点击编辑；`b20295a` 图片内联（markpdf-file 协议）；⌘F 查找、⌘+点击链接跳转、双击图片编辑 |
| FR-2.4 | KaTeX / 脚注 / 高亮 | ✅ | **mermaid 用户决定不做**。内核 `extended.js` 字符级扫描器（排除代码区/图片/表格范围）：行内 `$..$`+块级 `$$..$$` KaTeX 渲染（CSS/字体 esbuild 打包离线化，throwOnError:false + 异常降级源码）、脚注引用上标编号（首次引用顺序）+ 定义行标记隐藏、高亮 `==text==` 黄底（明暗两套变量）；replace 重叠经 extReplaces 抑制集根治；32 vitest（含刁钻文档装饰不重叠普查）；**限制：表格单元格内不渲染**；真机验收通过（2026-07-20） |
| FR-2.5 | 图片粘贴/拖拽入 assets | ✅ | 内核拦截 paste/drop（无图片时交 CM 默认），base64 经新消息 `editor.saveImage`（请求-响应）存工作区 `assets/`（唯一命名 + 原子写），回插相对路径链接；草稿/无工作区弹提示；md 跨目录移动自动重写相对图片链接（`MarkdownImageLinkRewriter`，外链/绝对路径不动；打开中的标签以磁盘为准重载）；13 单测；真机验收通过（2026-07-20） |
| FR-2.6 | 大纲 TOC 面板 | ✅ | `bd30e22`（内核 lezer 提取 + 点击跳转） |
| FR-2.7 | 自动保存 500ms 防抖 + 原子写盘 | ✅ | `ae1eecf` |
| FR-2.8 | 字数/阅读时长 | ✅ | `TextStatistics` 纯函数（CJK 逐字 + 西文按词，对齐 Typora 口径；字符数不含空白；400 字/分钟）；EditorStore.text didSet 实时更新；状态栏 md 上下文展示「N 字 · N 字符 · 约 N 分钟」；7 单测；真机验收通过（2026-07-20） |
| FR-2.9 | 导出 PDF / HTML | ✅ | 真机验收通过（2026-07-20）。内核 `editor.exportHTML`：打印模式离屏重渲染 + forceParsing 全文解析 + 样式收集（内联 style + KaTeX editor.css 绝对地址）+ markpdf-file 图片地址重写；独立离屏导出会话（MarkdownExportSession）；PDF 走 NSPrintOperation 打印通道（A4 + 48pt 边距，真实分页）；HTML 原子写盘；入口=工具栏导出菜单+文件菜单；10 vitest；**限制：分享出去的 HTML 的 KaTeX 样式依赖 file:// bundle 路径（本机查看正常）** |
| FR-2.10 | 打字机/专注模式 | 🚧 | 内核两 Compartment 独立开关（新消息 `editor.setTypewriter`/`setFocusMode`，去重推送）；打字机=选区变化 scrollIntoView 居中（异步派发防 updateListener 同步 dispatch）；专注=StateField 标记当前行 + CSS 压暗其余（.18s 过渡）；设置面板两个 Toggle + 命令面板两条命令，持久化；e2e 实测（焦点行/压暗数/居中偏差全过）；**待真机验收** |

## 模块 3：PDF 阅读

| 编号 | 需求 | 状态 | 提交 / 备注 |
|---|---|---|---|
| FR-3.1 | PDFKit 渲染 | ✅ | `8bd6262` 最小版；连续滚动 |
| FR-3.2 | 缩放 50%–400% | ✅ | `f8f3d29` 状态栏控件；`ae843d6` 捏合改响应链通道（根治失效）；GPU 变换 + 中心锚点 |
| FR-3.3 | 大纲 / 书签 / 缩略图侧栏 | ✅ | `857b973`（PDFThumbnailView + PDFOutline + 用户书签持久化） |
| FR-3.4 | 页内搜索 | ✅ | `13ab5d7`（防抖即搜、计数、⌘F/⌘G）；**查找栏修复批次**：① 修复 ⌘F 查找栏此前从未挂载（`PDFFindBarView` 无生产引用，⌘F 置 `isFindBarVisible` 后无 UI 响应，实际不可用）——现挂载为 PDF 内容顶部浮层；② 跳转 `go(to:page)`→`go(to:selection)`，放大后命中词在视口外也精确定位；③ 焦点在 PDF 上按 Esc 也能退出查找（`keyDown`/`cancelOperation` 双通道），查找栏打开时再按 ⌘F 经 `findFocusRequest` 令牌重新聚焦；④ md 查找栏（CodeMirror 面板）CSS 美化，向 PDF 栏 macOS 风格对齐；**本批真机验收通过（2026-07-21）** |
| FR-3.5 | 阅读位置记忆 | ✅ | `PDFReadingPositionStore` 按文件存页码+缩放（UserDefaults，仿书签模式）；翻页/缩放 0.5s 防抖落盘、视图拆除前 fire 兜底；重开恢复页码与缩放（无存档走自适应宽度）；Debouncer 新增 fire()；6 单测；真机验收通过（2026-07-20） |
| FR-3.6 | 阅读主题 | 🚧 | 白天/夜间两档（设置面板 + 命令面板，持久化，切换即时生效不重排）；夜间=CIColorInvert+CIHueAdjust 180°（Dark Reader 方案：亮度反转、色相保持，图片观感接近正常；新版 macOS 无 CIHueRotate，等价替代）。**羊皮纸档经用户决策移除**；**偏差：非内容流级反色**（PDFKit 无此能力；极端彩色图可能轻微失真）；**待真机验收** |

## 模块 4：PDF 标注（M2 进行中）

| 编号 | 需求 | 状态 | 提交 / 备注 |
|---|---|---|---|
| FR-4.1 | 文本标注（高亮/下划线/删除线/波浪线 + 划词浮动工具条） | ✅ | `b64d947`、`665ec08`（逐行创建防叠块）；**偏差：波浪线移除**（PDFKit 不支持 Squiggly 子类型，SDK 头注） |
| FR-4.2 | 便签 | ❌ | **需求移除**（用户决定不做；实现整体回退，AnnotationKind.note 枚举一并删除） |
| FR-4.3 | 批注（页边标记 + 点状虚线 + 高亮锚定） | ✅ | `87d4c55`：**需求精简**（用户决定：仅保留批注，手绘/矩形/箭头不做）：划词在页缘放 /Text 便签图标（位置只跟随内容块垂直中心，不遮挡正文），小高亮方块拼点状虚线直线斜连图标与内容块（PDFKit 不渲染程序化 Line/Ink，渲染探针实锤），选中文字逐行高亮；点击图标/高亮/虚线弹编辑框（NSTextView + 草稿一次性提交，打字零卡顿）；原生 Popup 弹窗三处压掉 + 图标点击走 mouseDown 拦截（PDFKit 会抢事件）；左右栏按内容块中线严格判定；写回 round-trip 单测 |
| FR-4.4 | 颜色系统（4 色 + 各类型记忆用色） | ✅ | `e452229`：工具栏 PDF 标注工具组（仅 PDF 上下文，对齐设计稿 #pdfTools）：工具按钮选中 = 划词即标，四色色板按类型记忆并持久化 UserDefaults，Esc 退出工具；浮动工具条用色点实时同步；4 单测 |
| FR-4.5 | 标注列表面板（排序筛选/定位/改名/删除） | ✅ | `670921b`：侧栏标注段实装（分段标签带计数）：同组合并为单条、摘录取自覆盖文本；按页/颜色/类型排序菜单；点击定位 + 覆盖层闪烁；右键改名（写 contents）/删除；Store.revision 驱动实时同步；8 单测 |
| FR-4.6 | 写回 PDF + .bak 备份 | ✅ | `d139178`（原子写回 + 防抖调度 + 3 单测） |
| FR-4.7 | 只读模式 sidecar | ✅ | `SidecarAnnotationStorage` 序列化（page/subtype/bounds/颜色 hex/contents/组 ID，Popup 伴侣按 type 排除——PDFKit 自动伴侣类名是 PDFAnnotation 而非 PDFAnnotationPopup，探针实锤）；`SidecarAnnotationWriter` 复用 writer 注入点（PDF 本体不动、无 .bak、原子写）；attach 按文件恢复模式并从 JSON 重建标注；文件菜单「只读标注模式」Toggle 逐文件切换并持久化；**限制：切换模式不迁移既有标注**；7 单测；真机验收通过（2026-07-20） |
| FR-4.8 | 导出标注为 Markdown | ✅ | 工具栏导出菜单（对齐设计稿 #btnExport，含 FR-2.9 占位项）+ 文件菜单命令；按页分组 `- [p.N] 摘录 — 批注`，追加写目标笔记、行内容精确去重；导出后自动打开笔记；原子写盘；11 单测；`639d53b`；真机验收通过（2026-07-20） |

## 模块 5：Markdown × PDF 联动

| 编号 | 需求 | 状态 | 提交 / 备注 |
|---|---|---|---|
| FR-5.1 | 分栏对照（左 PDF 右笔记） | ✅ | 由 FR-1.4 双标签组分栏架构覆盖（左栏 PDF、右栏笔记，独立滚动；用户确认现状满足，验收通过） |
| FR-5.2 | 复制生成回链引用块 | 🚧 | 编辑菜单「复制为带回链的引用」⇧⌘C（选区状态 PDFViewSelectionChanged 可观测回写 hasSelection）；格式 `> 引用` + `> — [名称 · p.N](根目录相对路径#page=N)`；**偏差：独立快捷键而非复制即生成**（避免污染 ⌘C 粘贴到其他应用）；**待真机验收** |
| FR-5.3 | md 中 PDF 回链跳转 | 🚧 | `MarkdownFileLink` 解析 `xxx.pdf#page=N`（md 目录优先、工作区根目录回退）；⌘+点击打开标签并跳页 + 整页边框闪烁 1s（`AnnotationFlasher.flashPage`）；pendingPage 在 updateNSView 也消费（已打开的 PDF 可跳）；**修复：回链跳转后左侧文件树高亮同步（此前仅开标签、文件树选中仍停在原 md）**；10 单测；**待真机验收** |
| FR-5.4 | 反向链接面板 | 🚧 | `BacklinksFinder` 纯函数（链接解析复用 FR-5.3 规则：md 目录优先、根目录回退；排除自引用/图片/外链）；`BacklinksStore` 防抖后台扫描 + FSEvents 刷新重扫（新引用 5s 内）；md 上下文右面板=大纲+反向链接，PDF 侧栏新增「引用」段；点击打开来源；6 单测；**待真机验收** |

## 模块 6：搜索与导航

| 编号 | 需求 | 状态 | 提交 / 备注 |
|---|---|---|---|
| FR-6.1 | ⌘P 文件名模糊搜索 | ✅ | `9cf69e9`（子序列评分 + 键盘导航） |
| FR-6.2 | 全文搜索 | ✅ | ⌘⇧F 浮层（对齐 QuickOpen 形态）：md 按行逐处匹配 + PDF 页文本提取，上下文摘录 + 命中数，相关度降序，0.3s 防抖 + 后台执行 + 取消；命中跳转 md 跳行（内核未就绪滚动请求排队补发，修复此前丢弃问题）/ pdf 跳页（pendingPage 优先于位置恢复）；8 单测；真机验收通过（2026-07-20） |
| FR-6.3 | 命令面板 | 🚧 | ⌘O 面板（「打开文件夹」让位 ⌘⇧O）：约 24 条命令（文件/导出/视图/PDF/其他）带上下文启用条件；标题 + 拼音首字母双通道模糊匹配（`PinyinInitials` 走 CFStringTransform 转拉丁，逐字取首字母，无拼音表）；↑↓/Enter/Esc；5 单测；**待真机验收** |

## 模块 7：外观与设置

| 编号 | 需求 | 状态 | 提交 / 备注 |
|---|---|---|---|
| FR-7.1 | 明暗主题跟随系统 | ✅ | 内核 CSS 变量 + 桥接 setTheme |
| FR-7.2 | 字体/字号/行高与默认视图设置 | ✅ | `SettingsStore`（UserDefaults 持久化）+ ⌘, Settings 面板；内核字号/行高/字体改 CSS 变量（新消息 `editor.setTypography`，去重推送，即时生效）；PDF 默认视图（连续/单页/双页连续）创建与切换即时应用；4 单测；真机验收通过（2026-07-20） |

---

## 基础设施修复记录（非 FR）

| 事项 | 提交 / 备注 |
|---|---|
| 沙盒 network.client 授权（WKWebView 启动必需，否则编辑器空白） | `203d406`；与 §10「不申请网络权限」的偏差已评审：App 零网络请求 |
| CM6 块级装饰改 StateField（Block decorations 不可经插件） | 同上 |
| bash/go/swift 语言包 LanguageSupport 包裹（解析崩溃） | 同上 |
| 标题行垂直 margin→padding（坐标映射累积漂移） | 同上 |
| WKWebView 非持久数据存储（旧 bundle 缓存） | 同上 |
| "Publishing changes from within view updates" 系列修复 | `8f7d822` 等 |
| 图片预览缩放 + SVG 预栅格化 | `c70af48`、`24b9989` |
| 退出时 PDF 标注写回 flush 兜底（此前 500ms 防抖窗口内退出会丢标注） | `639d53b` |
| Debouncer.fire 顺序 bug（先 cancel 后 perform 为空操作，影响 FR-3.5/退出兜底） | `4a50e74`，附回归测试 |
| 文内锚点链接（#标题）⌘+点击不跳转（此前一律发 native 走 http 白名单被挡；内核按 GitHub slug 匹配标题滚动） | `a4af493`；e2e 两跳全中；6 vitest |
| 导出内容只有首屏（CM 普通渲染态视口=窗口内可见部分，离屏容器花招均无效；正解为 CM 打印模式 `viewState.printing=true`+`measure()`，1246 行实测全量导出） | `a4af493`；Chromium 探针逐帧定位 + 截图目检 |
| 导出后半段变源码（新建视图 lezer 增量解析未完成，装饰只覆盖已解析前缀；正解 `forceParsing` 强制同步解析） | `d9ae3ef`；真实文档离屏 WKWebView 探针复现并验证 |
| 导出 PDF 整篇挤成一两页超长页（createPDF 是内容快照不做打印分页；改 NSPrintOperation 打印通道，A4 48pt 边距真实分页） | `d9ae3ef`；真实文档打印 21 页 A4，真机验收通过 |
| 导出按钮置灰不刷新（kernel 弱引用不可观测）→ 改 md 标签激活即可用；继而发现「内核尚未就绪」——EditorStore.kernel 反引方案弃用，改每次导出创建独立离屏导出会话（MarkdownExportSession，对齐 Electron 隐藏窗口 + printToPDF 成熟架构） | `5021608`、`d9ae3ef` |
| 反向链接扫描越界崩溃（UTF-16 下标误当字符下标，多字节文本 fatal error）与工作区 node_modules 扫描卡死 | `0fa7a76`：Range 转换取字符 + 多字节回归测试；扫描排除 node_modules/.git 等重型目录；反链文件名预筛 |
| 阅读主题羊皮纸=白天、夜间退化为纯反色（新版 macOS 移除 CIColorMultiply/CIHueRotate，滤镜创建返回 nil 静默失效） | `0fa7a76`：CIMultiplyCompositing/CIHueAdjust 等价替代，CIImage 探针验证色值 |
| 专注模式点击即失效（CM 每次交易重建 view.dom.className，手动挂的开关类被抹掉） | `2db5e39`：开关类改挂 documentElement（含 ⌘ 链接手型 cm-mod-down）；Chromium 探针复现并验证 |

| ⌘F 查找面板 ↑↓ 不导航（CM findNext 末尾 selectSearchInput 抢焦点触发 DOM 选区同步竞态；改直接按 QueryType 游标派发选区） | `4316277`；e2e 轨迹实测；真机验收通过 |
| 内核 demo 初始文档在 App 内误显示（setContent 握手丢失时的误导性内容；按 ?app=1 区分，App 内初始为空） | `2b183b7` |
| 导出标注行升级页码回链（`- [p.N](path#page=N)`，相对目标笔记目录）；合并时旧格式行同页同文就地升级、手改行不动 | `2b183b7`、`d2a9097`；真机验收通过 |
| 导出样式回退 CM 默认 monospace（外壳缺主题 hash 类 + 样式收集时机过早 + editor.css 相对路径 404） | 同上，外壳带真实类名、销毁前收集、el.href 绝对地址；导出页改整页流式布局 |
| PDF ⌘F 查找栏从未挂载（`PDFFindBarView` 无生产引用，⌘F 置 `isFindBarVisible` 后无 UI 响应）→ 挂载到 `TabGroupPane` 的 PDF 内容顶部浮层 | 本次提交 |
| PDF 查找跳转仅翻页不定位（`go(to:page)` 只对齐页顶，放大后命中词在视口外看不到）→ `go(to:selection)` | 本次提交 |
| PDF 查找栏 Esc 仅在查找框聚焦时可退出（焦点在 PDF 上失效）→ `ZoomablePDFView` 拦截 Esc（keyCode 53 `keyDown` + `cancelOperation` 双通道）；再按 ⌘F 经 `findFocusRequest` 令牌重新聚焦 | 本次提交 |
| 文件树高亮不跟随非文件树入口（回链/全文搜索/导出打开/切标签/切分栏组）→ 统一到 `TabStore.onOpenFile` + `onStructureChange` 同步 `WorkspaceStore.selection`（始终跟随当前激活标签） | 本次提交 |
| md 查找栏（CodeMirror 默认面板）样式粗糙 → `index.html` CSS 美化，向 PDF 查找栏 macOS 风格对齐（保留查找/替换/正则/整词功能） | 本次提交 |
| 标注写回降级路径数据丢失：`replaceItemAt` 失败后 tmp 状态未定义（可能被消耗或留损坏文件，探针实锤）+ defer 无条件删 tmp，最坏仅剩无标注 .bak → 降级前总是重写 tmp，`moveItem` 失败保留 tmp 救援文件 | 回归测试：注入 ObjC 层双失败 FileManager mock 锁定最坏场景 |
| 读写/写回失败静默（违反 NFR-5）：`EditorStore` 打开/自动保存失败、`PDFAnnotationStore` 写回失败仅记日志 → `lastError` + alert 用户可感知；持续失败只提示一次（防弹窗轰炸，恢复后复位） | 新增 `EditorStoreTests`；alert 挂载 md 每标签就近 + 标注全局唯一 |
| 跨文档 ⌘Z 串档/清空文件（恶性）：单 WebView 换档 `setContent` 被记入撤销栈且旧文件历史残留，⌘Z 撤销「换档」把上一文档写回当前文件 → 换档 `addToHistory:false` + history 放 Compartment 换档即重置 | 浏览器实测：换档后 ⌘Z 无效果、正常输入 ⌘Z 可用；vitest 46 过 |
| 代码块视觉重做：四周描边（对齐表格）、语言选择下拉（隐形原生 select 覆盖徽标，选中改写 fence 语言标识即时切高亮）、复制按钮（图标，Clipboard API + execCommand 双兜底）、GitHub 排版规格（ui-monospace 栈/14px/行高 1.6/圆角 6px）、注释去伪斜体（CJK 无真斜体字形）、语言栏固定 28px（消除 CJK 行盒撑高的真机/浏览器差异） | 无头 Chrome 截图逐版迭代验收（含暗色） |
| 编辑态末尾 ``` 与上一行重叠：`cm-codeblock-last` 的 `line-height:0` 误伤编辑态源码行 → `:has(.cm-fence-end)` 区分渲染/编辑两态 | 截图验证编辑态 ``` 独立成行 |
| 选区高亮三修复：① 不透明代码块底色盖住 drawSelection 选层（框内看不到选区）→ 半透明 rgba 底色；② drawSelection 整行色块（选区远超文字范围）→ `selTextField` mark 装饰文本级高亮（贴合文字、跨行按行截断）；③ CM 内建聚焦态 5 类选择器规则（`#d7d4f0`）打败 2 类透明规则造成双层高亮 → 同优先级全路径覆盖 | 注入 CSS 转储实证规则顺序；截图验证单层贴合文字 |
| PDF 大文档打开整窗卡顿（主线程同步 `PDFDocument(url:)` 解析，237 页实测 ~1s）→ `Task.detached` 后台解析 + 代际号防串档 + 加载旋转指示；待跳转页在文档未就绪时不消费（防丢失） | NFR-1；异步通道同 FullTextSearch 先例 |
| 脚注双向跳转（FR-2.4 增强）：引用↔定义按 label 唯一对应（多对一），点文中上标 `[n]` 跳定义行、点定义行尾 `↩` 回跳第一处引用；上标双击仍落光标编辑源码 | 截图验证渲染；jumpToLine 共用 |
| **批次一·数据安全（review 后修复）** 内核 300ms 上报防抖窗口丢尾输入（打完立刻切标签/⌘W/⌘Q，JS 未上报的尾巴随 WebContent 进程销毁丢失）→ `pagehide`/`visibilitychange(→hidden)` 立即 flush 挂起 notify + `dismantleNSView` 经 getContent 取回全文兜底落盘 | 本次提交；jsdom 事件回归 3 例，vitest 49 过 |
| restore 重放丢草稿（红钮关窗→Dock 重开，onAppear 再次 restore 整体替换 groups，内存草稿正文被丢弃）→ `TabStore.didRestore` 一次性守卫 | 本次提交；`TabStoreRestoreTests` |
| 废纸篓恢复后重开永不落盘 + 橙点常亮（删除转草稿后缓存 store 不重载，`guard let url` 静默吞保存）→ `trashedFileURL` 标记 + 重开命中缓存时重新 loadFile；转草稿即清脏标/取消 pendingSave | 本次提交；`TrashRestoreTests` 2 例 |
| 撤销重命名/移动不同步标签（undo 绕过 `fileDidMove`，标签指新名、自动保存在旧名重建文件，双文件数据分叉）→ `WorkspaceStore.onFileMoved` 统一回调覆盖 undo/redo 全链，视图层重复通知清理 | 本次提交；新建 `WorkspaceStoreTests` 2 例 |
| 撤销「新建文件夹」重做出空文件（createFolder/createMarkdown 共用 undo 链，recreate 一律 createFile）→ undo 注册记录创建类型分派 | 本次提交；`WorkspaceStoreTests` 2 例 |
| 仅大小写变化的重命名必失败（大小写不敏感 FS 上 fileExists 误报 alreadyExists）→ `caseInsensitiveCompare` 豁免（与另一文件仅大小写相同仍正确拒绝） | 本次提交；`FileOperationsTests` 2 例 |
| PDF 标注防抖窗口内切档/关窗丢写回（weak document 先归零，flush 静默 return）→ 清 document/拆视图/切 sidecar 通道前一律先 `flushPendingWrites()` | 本次提交；`PDFAnnotationStoreTests` 2 例 |
| 导出标注整体覆盖读不出的既有文件（`try?` 混淆「不存在」与「UTF-16 读失败」）→ `fileExists` 先区分，读失败弹错中止不写盘 | 本次提交；`AnnotationExporterTests` 3 例 |
| 反向链接三连漏报：① App 自产 %20 回链被未编码文件名预筛跳过（含空格/中文名目标永远漏）；② APFS 大小写不敏感但终比对精确相等；③ CommonMark 角标 `<含空格 dest>` 截断 → 预筛双变体 + 大小写不敏感 + 模式角标分支 | 本次提交；`BacklinksFinderTests` 3 例 |
| **批次二·分栏正确性（review 后修复）** 同一文件可在左右分栏双开（TabGroup.open 去重只查本组），两套 EditorStore 各自自动保存互相覆盖 → `TabStore.open` 先全组查找，命中即切组激活不新建 | 本次提交；`TabStoreCrossGroupOpenTests` 3 例 |
| 分栏双 PDF 共享 App 级标注 Store，A 窗标注写进 B 文档且永不落盘（后加载 pane 覆盖 document/url）→ Store 指向跟随既有焦点仲裁：mouseDown 认领焦点即 attach 本 pane（加载完成不再抢关联），attach 换目标前先 flush 旧目标 | 本次提交；`PDFAnnotationStoreTests` 2 例 |
| `pendingPage` 不带目标 URL，分栏开 PDF-A 时点回链到 PDF-B 被 A 抢跳 → 改 `pendingJump:(url,page)` 按 URL 匹配消费，且直接导航本视图（不经焦点视图的 store.goTo） | 本次提交；`PDFReaderStoreTests` 4 例 |
| **批次三·性能（review 后修复）** 保存风暴：每次自动保存→FSEvent→全量目录重扫+反链重读全部 md（node_modules 内事件也触发）→ FileWatcher 按事件路径过滤排除目录子树（`WorkspaceExcludedDirectories` 共享名单）；反链重扫分流到 md 文件集合变化时（`onMarkdownFilesChange`），内容保存/折叠展开不再重读 | 本次提交；`FileWatcherFilterTests` 9 例 |
| 大工作区扫描取消粒度：BacklinksFinder 无取消点（cancel 后仍读完全部 md）→ 逐文件 `isCancelled`（同 FullTextSearch 口径）；全文搜索 PDF 无上限、页循环无取消 → 100MB 上限 + 每 20 页查取消 | 本次提交；回归 6 例 |
| 字数统计每击键 O(n) 全量扫描（主线程毛刺源）→ 0.5s 防抖 + 后台串行队列计算回填；EditorStore 文件读写移出主线程（loadFile 代际号防串档、写盘串行队列 + flush 屏障，切走文件后过期写不污染新状态） | 本次提交；`EditorStoreAsyncIOTests` 7 例 |
| 图片预览主线程同步解码/栅格化（大图切标签即卡）→ 后台解码 + 代际号作废在途结果，旧图保持显示到解码完成 | 本次提交 |
| 内核装饰层每次交易全量重建（纯方向键/点击也全文三轮正则扫描，88KB 文档 ~18ms）→ 重活按 `(doc, syntaxTree)` 引用记忆化，纯选区交易复用扫描结果；doc/tree 引用变即自动失效 | 本次提交；`wysiwyg-memo.test.js` 3 例（扫描计数 + 输出对齐） |
| 标注列表/侧栏计数在 body 内全文档重扫（切色板都触发）→ store 内 `annotationItemsSnapshot` 缓存仅随 revision 重算（导出口径仍实时全扫）；QuickOpen/命令面板 results 改缓存消除同帧重复计算，`FuzzyQuery` 预处理上提，同分 tiebreak 定序；反链面板 ForEach 重复 id → enumerated | 本次提交；`AnnotationItemsCacheTests` 4 例 + `SearchPanelResultsTests` 6 例 |
| **批次四·体验（review 后修复）** 脚注 `[^注](2024)` 写法触发 CM 禁止的装饰重叠（lezer 解析为 Link 与脚注扫描相交）→ 含 URL 子节点的 Link 范围纳入脚注排除，整体按链接渲染；纯 `[^a]` 照常上标 | 本次提交；extended/wysiwyg 回归 4 例 |
| 橙点/字数/模式选择器不实时刷新（嵌套 ObservableObject 不冒泡）→ EditorStore 显式 @ObservedObject 注入 TabBadge/MarkdownStatus/EditorModePicker；`editorStore(for:)` 不再在 body 期建 store/发布（动作阶段 prepareStore 预建 + 兜底延迟注册）；RecentFiles 读路径不再写 @Published | 本次提交；`TabGroupTests` 9 例 |
| 切文档不重置查找状态（旧文档 PDFSelection 可作用于新文档）与缩放继承竞态（新文档误失 autoScales）→ `resetForDocumentSwitch` 统一复位 + 加载窗口期缩放同步守卫；PDF 解析失败占位 + 重试（此前永久空白） | 本次提交；`PDFReaderStoreTests` 3 例 + `ViewLayerLogicTests` |
| sidecar 损坏静默吞标注（违反 NFR-5）→ 解码改 throws + lastError 上报（只提示一次）+ 损坏期间禁写回防覆盖原文件；批注含 emoji 时光标错位（NSRange 用 Character 计数）→ UTF-16 长度；死书签防护（page≥1）；标注组改名失焦即提交（Esc 仍取消） | 本次提交；`SidecarAnnotationTests` 2 例等 |
| 工作区体验：标签栏横向滚动（激活标签自动滚动可见）；⌘Q 兜底落盘挪 App init（无窗口也生效）；拖拽工作区外文件明确拒绝 + 多项逐个处理；移动/删除文件夹后代标签前缀迁移（此前标签指旧路径保存报错）；重命名换扩展名重算标签 kind | 本次提交；`TabGroupTests` 覆盖 |
| 杂项：查找面板 ↑↓ 回绕；scrollToLine 非整数/NaN 防御；表格单元格 code+link 类名叠加；extractTitle `# C#` 误判；escapeHTML 补双引号；图片粘贴失败留诊断；排除名单补 Pods/.venv/dist/build 等且大小写不敏感；欢迎文档示例 API 改正；fetchContent 失败回调（非死代码，为销毁前兜底链路） | 本次提交；vitest 62 过 + `FileWatcherExcludeExtraTests` 4 例 |
| 启动竞态：恢复的标签文件全部「无权限读取」弹窗（批次三 loadFile 异步化 + 批次四 restore 预建立即读，与 onAppear 里「先 restoreTabs 后 restoreWorkspace 授权」的既有顺序赛跑，bookmark 解析窗口内后台读全 EPERM；日志实证爆发后授权建立即恢复）→ onAppear 先 restoreWorkspace 建立沙盒授权再 restoreTabs | 本次提交；真机验收（重启 App 无弹窗、恢复标签正常载入） |

## 已知限制与偏差

1. **波浪线（FR-4.1）**：PDFKit 不支持 Squiggly，已从工具条移除（Ink 模拟方案弃用）。
2. **便签（FR-4.2）**：用户决定不做，需求与实现整体移除（含 AnnotationKind.note）。
3. **图形标注（FR-4.3）**：用户决定精简为「批注」一种（页边文本框 + 虚线连接器）；手绘/矩形/箭头不做，对应枚举已删除。
3b. **mermaid（FR-2.4）**：用户决定不做（体积最大、集成最复杂）；FR-2.4 其余三项（公式/脚注/高亮）已实现。
4. **标签切换撤销栈**：换档即重置内核撤销历史（修复跨文档 ⌘Z 串档后正式生效——切回不保留 ⌘Z，但内容经 0.5s 自动保存绝不丢失）；按文档保留撤销历史可后续评估（内核 EditorState 池 / 多 WebView 池）。
5. **分栏双 PDF**：缩放/搜索/标注列表与写回均作用于最近点击的视图（标注 Store 跟随焦点仲裁，切换前先落盘旧文档）。
6. ~~**CI**~~：已启用（`.github/workflows/macos.yml`，2026-07-21）。
7. **FR-1.6 偏差**：侧栏宽度与分栏比例不持久（macOS 13 无宽度观测 API）；草稿内容不持久。
8. **FR-3.6 偏差**：夜间主题为图层滤镜方案（非内容流级反色），极端彩色图可能轻微失真。
9. **导出 HTML 分享**：KaTeX 样式依赖 file:// bundle 路径（本机查看正常，分享他机公式样式丢失）。
10. **md 查找栏样式**：为保留 CodeMirror 查找/替换/正则/整词功能，仅 CSS 美化向 PDF 栏靠拢，非像素级一致（无放大镜图标与 x/n 计数——那需自绘面板重做）。
