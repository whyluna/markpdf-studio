# MarkPDF Studio 开发进度

> 与《需求文档 v1.0》（FR-x.x 编号）逐项对应。
> **更新约定**：每完成/变更一项即更新本文件并随代码同提交（开发规范 §5）。

| 图例 | 含义 |
|---|---|
| ✅ | 完成（含真机验证） |
| 🚧 | 进行中 / 部分完成 |
| ⬜ | 未开始 |
| ⏸️ | 延后（按 PRD 排期在后续里程碑） |

## 里程碑总览

| 里程碑 | 内容 | 状态 |
|---|---|---|
| M1 · MVP | 模块 1 全部 P0、FR-2.1/2.2/2.3/2.6/2.7、FR-3.1~3.4、FR-6.1、FR-7.1 | ✅ 已验收（2026-07-18） |
| M2 · 标注 | 模块 4 全部 P0、FR-4.3、FR-5.1 | ✅ 已验收（2026-07-19；FR-4.2 便签经决策移除） |
| M3 · 联动与完善 | 剩余 P1 | 🚧 开发完成（11 项 FR + 3 修复，全量单测绿），待真机验收 |
| M4 · 打磨 | P2、性能、签名分发 | ⬜ |

---

## 模块 1：文件与项目管理

| 编号 | 需求 | 状态 | 提交 / 备注 |
|---|---|---|---|
| FR-1.1 | 工作区文件树 | ✅ | `8bd6262`；后续交互修复（整行点击/新建文件夹可见） |
| FR-1.2 | 文件树操作（新建/重命名/删除/拖拽/右键/Cmd+Z） | ✅ | `65dd8c7`、`34287cb`（改名交互/打开中文件跟随） |
| FR-1.3 | FSEvents 外部变更自动刷新 | ✅ | `f5f0e97` |
| FR-1.4 | 标签页 + 左右分栏 | ✅ | `62a1a3e`、`8f7d822`；限制：标签切换内核撤销栈重置 |
| FR-1.5 | 收藏夹、最近打开 | 🚧 | `FavoritesStore`/`RecentFilesStore`（按工作区根路径存 UserDefaults，最近上限 20 条去重置顶）；文件树新增「收藏/最近打开」分区（对齐设计稿 .sec，已删文件不显示）；右键「加入收藏」；`TabStore.onOpenFile` 钩子集中记录；FileNode.Kind 下图标属性；8 单测；**待真机验收** |
| FR-1.6 | 工作区状态持久化 | 🚧 | `WorkspaceStateStore`：快照 JSON 存 UserDefaults（0.5s 防抖 + 退出 flush）；根目录存 security-scoped bookmark（新增 app-scope entitlement，恢复后保持访问权）；标签组/分栏/激活状态重建（空组丢弃、全空保留草稿）；md 光标行经内核 `editor.cursor` 上报 + setContent `initialLine` 恢复；PDF 位置由 FR-3.5 覆盖；Debouncer.fire 修复（先 cancel 后 perform 为空操作的 bug，附回归测试）；6+2 单测；**待真机验收**。**偏差：侧栏宽度与分栏比例不持久**（NavigationSplitView/HSplitView 在 macOS 13 无宽度观测 API）；**草稿内容不持久**（恢复为空草稿） |

## 模块 2：Markdown 编辑与阅读

| 编号 | 需求 | 状态 | 提交 / 备注 |
|---|---|---|---|
| FR-2.1 | 所见即所得编辑 | ✅ | 光标映射修复（CM6 行高测量 margin 漂移）、drawSelection 自绘光标 |
| FR-2.2 | 三模式切换 | ✅ | 所见即所得 / 源码 / 阅读 |
| FR-2.3 | GFM 全集 | ✅ | `203d406` 表格渲染+点击编辑；`b20295a` 图片内联（markpdf-file 协议）；⌘F 查找、⌘+点击链接跳转、双击图片编辑 |
| FR-2.4 | KaTeX / 脚注 / 高亮 | 🚧 | **mermaid 用户决定不做**。内核 `extended.js` 字符级扫描器（排除代码区/图片/表格范围）：行内 `$..$`+块级 `$$..$$` KaTeX 渲染（CSS/字体 esbuild 打包离线化，throwOnError:false + 异常降级源码）、脚注引用上标编号（首次引用顺序）+ 定义行标记隐藏、高亮 `==text==` 黄底（明暗两套变量）；replace 重叠经 extReplaces 抑制集根治；32 vitest（含刁钻文档装饰不重叠普查）；**限制：表格单元格内不渲染**；**待真机验收** |
| FR-2.5 | 图片粘贴/拖拽入 assets | 🚧 | 内核拦截 paste/drop（无图片时交 CM 默认），base64 经新消息 `editor.saveImage`（请求-响应）存工作区 `assets/`（唯一命名 + 原子写），回插相对路径链接；草稿/无工作区弹提示；md 跨目录移动自动重写相对图片链接（`MarkdownImageLinkRewriter`，外链/绝对路径不动；打开中的标签以磁盘为准重载）；13 单测；**待真机验收** |
| FR-2.6 | 大纲 TOC 面板 | ✅ | `bd30e22`（内核 lezer 提取 + 点击跳转） |
| FR-2.7 | 自动保存 500ms 防抖 + 原子写盘 | ✅ | `ae1eecf` |
| FR-2.8 | 字数/阅读时长 | 🚧 | `TextStatistics` 纯函数（CJK 逐字 + 西文按词，对齐 Typora 口径；字符数不含空白；400 字/分钟）；EditorStore.text didSet 实时更新；状态栏 md 上下文展示「N 字 · N 字符 · 约 N 分钟」；7 单测；**待真机验收** |
| FR-2.9 | 导出 PDF / HTML | 🚧 | 内核 `editor.exportHTML`：离屏 reading 模式重渲染（游标块不露源码），样式收集（内联 style + KaTeX editor.css 链接），markpdf-file 图片转相对/file:// 地址，标题取首个标题；native 经 `EditorStore.kernel` 请求，HTML 原子写盘，PDF 离屏 WKWebView createPDF（临时隐藏 HTML 写 md 目录保图片相对路径，自保持池 + 0.4s 图片落定）；入口=工具栏导出菜单+文件菜单；10 vitest（jsdom 纯函数）；**限制：分享出去的 HTML 的 KaTeX 样式依赖 file:// bundle 路径（本机查看正常）**；**待真机验收** |
| FR-2.10 | 打字机/专注模式 | ⏸️ | P2 → M4 |

## 模块 3：PDF 阅读

| 编号 | 需求 | 状态 | 提交 / 备注 |
|---|---|---|---|
| FR-3.1 | PDFKit 渲染 | ✅ | `8bd6262` 最小版；连续滚动 |
| FR-3.2 | 缩放 50%–400% | ✅ | `f8f3d29` 状态栏控件；`ae843d6` 捏合改响应链通道（根治失效）；GPU 变换 + 中心锚点 |
| FR-3.3 | 大纲 / 书签 / 缩略图侧栏 | ✅ | `857b973`（PDFThumbnailView + PDFOutline + 用户书签持久化） |
| FR-3.4 | 页内搜索 | ✅ | `13ab5d7`（防抖即搜、计数、⌘F/⌘G） |
| FR-3.5 | 阅读位置记忆 | 🚧 | `PDFReadingPositionStore` 按文件存页码+缩放（UserDefaults，仿书签模式）；翻页/缩放 0.5s 防抖落盘、视图拆除前 fire 兜底；重开恢复页码与缩放（无存档走自适应宽度）；Debouncer 新增 fire()；6 单测；**待真机验收** |
| FR-3.6 | 阅读主题 | ⏸️ | P2 → M4 |

## 模块 4：PDF 标注（M2 进行中）

| 编号 | 需求 | 状态 | 提交 / 备注 |
|---|---|---|---|
| FR-4.1 | 文本标注（高亮/下划线/删除线/波浪线 + 划词浮动工具条） | ✅ | `b64d947`、`665ec08`（逐行创建防叠块）；**偏差：波浪线移除**（PDFKit 不支持 Squiggly 子类型，SDK 头注） |
| FR-4.2 | 便签 | ❌ | **需求移除**（用户决定不做；实现整体回退，AnnotationKind.note 枚举一并删除） |
| FR-4.3 | 批注（页边标记 + 点状虚线 + 高亮锚定） | ✅ | `87d4c55`：**需求精简**（用户决定：仅保留批注，手绘/矩形/箭头不做）：划词在页缘放 /Text 便签图标（位置只跟随内容块垂直中心，不遮挡正文），小高亮方块拼点状虚线直线斜连图标与内容块（PDFKit 不渲染程序化 Line/Ink，渲染探针实锤），选中文字逐行高亮；点击图标/高亮/虚线弹编辑框（NSTextView + 草稿一次性提交，打字零卡顿）；原生 Popup 弹窗三处压掉 + 图标点击走 mouseDown 拦截（PDFKit 会抢事件）；左右栏按内容块中线严格判定；写回 round-trip 单测 |
| FR-4.4 | 颜色系统（4 色 + 各类型记忆用色） | ✅ | `e452229`：工具栏 PDF 标注工具组（仅 PDF 上下文，对齐设计稿 #pdfTools）：工具按钮选中 = 划词即标，四色色板按类型记忆并持久化 UserDefaults，Esc 退出工具；浮动工具条用色点实时同步；4 单测 |
| FR-4.5 | 标注列表面板（排序筛选/定位/改名/删除） | ✅ | `670921b`：侧栏标注段实装（分段标签带计数）：同组合并为单条、摘录取自覆盖文本；按页/颜色/类型排序菜单；点击定位 + 覆盖层闪烁；右键改名（写 contents）/删除；Store.revision 驱动实时同步；8 单测 |
| FR-4.6 | 写回 PDF + .bak 备份 | ✅ | `d139178`（原子写回 + 防抖调度 + 3 单测） |
| FR-4.7 | 只读模式 sidecar | 🚧 | `SidecarAnnotationStorage` 序列化（page/subtype/bounds/颜色 hex/contents/组 ID，Popup 伴侣按 type 排除——PDFKit 自动伴侣类名是 PDFAnnotation 而非 PDFAnnotationPopup，探针实锤）；`SidecarAnnotationWriter` 复用 writer 注入点（PDF 本体不动、无 .bak、原子写）；attach 按文件恢复模式并从 JSON 重建标注；文件菜单「只读标注模式」Toggle 逐文件切换并持久化；**限制：切换模式不迁移既有标注**；7 单测；**待真机验收** |
| FR-4.8 | 导出标注为 Markdown | 🚧 | 工具栏导出菜单（对齐设计稿 #btnExport，含 FR-2.9 占位项）+ 文件菜单命令；按页分组 `- [p.N] 摘录 — 批注`，追加写目标笔记、行内容精确去重；导出后自动打开笔记；原子写盘；11 单测；`639d53b`；**待真机验收** |

## 模块 5：Markdown × PDF 联动

| 编号 | 需求 | 状态 | 提交 / 备注 |
|---|---|---|---|
| FR-5.1 | 分栏对照（左 PDF 右笔记） | ✅ | 由 FR-1.4 双标签组分栏架构覆盖（左栏 PDF、右栏笔记，独立滚动；用户确认现状满足，验收通过） |
| FR-5.2 | 复制生成回链引用块 | ⏸️ | P2 → M3 |
| FR-5.3 | md 中 PDF 回链跳转 | ⏸️ | P2 → M3 |
| FR-5.4 | 反向链接面板 | ⏸️ | P2 → M3 |

## 模块 6：搜索与导航

| 编号 | 需求 | 状态 | 提交 / 备注 |
|---|---|---|---|
| FR-6.1 | ⌘P 文件名模糊搜索 | ✅ | `9cf69e9`（子序列评分 + 键盘导航） |
| FR-6.2 | 全文搜索 | 🚧 | ⌘⇧F 浮层（对齐 QuickOpen 形态）：md 按行逐处匹配 + PDF 页文本提取，上下文摘录 + 命中数，相关度降序，0.3s 防抖 + 后台执行 + 取消；命中跳转 md 跳行（内核未就绪滚动请求排队补发，修复此前丢弃问题）/ pdf 跳页（pendingPage 优先于位置恢复）；8 单测；**待真机验收** |
| FR-6.3 | 命令面板 | ⏸️ | P2 → M3 |

## 模块 7：外观与设置

| 编号 | 需求 | 状态 | 提交 / 备注 |
|---|---|---|---|
| FR-7.1 | 明暗主题跟随系统 | ✅ | 内核 CSS 变量 + 桥接 setTheme |
| FR-7.2 | 字体/字号/行高与默认视图设置 | 🚧 | `SettingsStore`（UserDefaults 持久化）+ ⌘, Settings 面板；内核字号/行高/字体改 CSS 变量（新消息 `editor.setTypography`，去重推送，即时生效）；PDF 默认视图（连续/单页/双页连续）创建与切换即时应用；4 单测；**待真机验收** |

---

## 基础设施修复记录（非 FR）

| 事项 | 提交 / 备注 |
|---|---|
| 沙盒 network.client 授权（WKWebView 启动必需，否则编辑器空白） | `203d406`；与 §10「不申请网络权限」的偏差已评审：App 零网络请求 |
| CM6 块级装饰改 StateField（Block decorations 不可经插件） | 同上 |
| bash/go/swift 语言包 LanguageSupport 包裹（解析崩溃） | 同上 |
| 标题行垂直 margin→padding（坐标映射累积漂移） | 同上 |
| WKWebView 非持久数据存储（旧 bundle 缓存） | 同上 |
| "Publishing changes from within view updates" 系列修复 | `8f7d822` 等 |
| 图片预览缩放 + SVG 预栅格化 | `c70af48`、`24b9989` |
| 退出时 PDF 标注写回 flush 兜底（此前 500ms 防抖窗口内退出会丢标注） | `639d53b` |
| Debouncer.fire 顺序 bug（先 cancel 后 perform 为空操作，影响 FR-3.5/退出兜底） | `4a50e74`，附回归测试 |
| 文内锚点链接（#标题）⌘+点击不跳转（此前一律发 native 走 http 白名单被挡；内核按 GitHub slug 匹配标题滚动） | 本地验证：e2e 两跳全中；6 vitest |
| 导出内容只有首屏（CM 普通渲染态视口=窗口内可见部分，离屏容器花招均无效；正解为 CM 打印模式 `viewState.printing=true`+`measure()`，1246 行实测全量导出） | 本地验证：Chromium 探针逐帧定位 + 截图目检 |
| 导出样式回退 CM 默认 monospace（外壳缺主题 hash 类 + 样式收集时机过早 + editor.css 相对路径 404） | 同上，外壳带真实类名、销毁前收集、el.href 绝对地址；导出页改整页流式布局 |

## 已知限制与偏差

1. **波浪线（FR-4.1）**：PDFKit 不支持 Squiggly，已从工具条移除（Ink 模拟方案弃用）。
2. **便签（FR-4.2）**：用户决定不做，需求与实现整体移除（含 AnnotationKind.note）。
3. **图形标注（FR-4.3）**：用户决定精简为「批注」一种（页边文本框 + 虚线连接器）；手绘/矩形/箭头不做，对应枚举已删除。
3b. **mermaid（FR-2.4）**：用户决定不做（体积最大、集成最复杂）；FR-2.4 其余三项（公式/脚注/高亮）已实现。
4. **标签切换撤销栈**：单 WKWebView 换内容导致内核撤销历史重置（后续可评估多 WebView 池）。
5. **分栏双 PDF**：缩放/搜索作用于最近点击的视图。
6. **CI**：`docs/macos-ci.yml` 模板未启用（需复制到 `.github/workflows/` 手动添加一次）。
