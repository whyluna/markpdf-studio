# Changelog

本文件遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 格式。

## [未发布]

### 新增

- **点选标注即可改色**：点击已有高亮/下划线/删除线，虚线框旁的编辑条除删除外新增四色选择（与顶部色板同一套黄/绿/蓝/红），点一下即改、连续试色位置不跳
- **批注编辑框键位**：回车保存并退出（等同点空白处），`⌘↵` 换行

### 修复

- **部分页面点正文冒出蓝色方框且无法标注**：这些页的批注留有 PDFKit 自动生成的 Popup 伴侣窗（128×64pt，压在正文上），它参与命中测试 → PDFView 给它画带手柄的选中框并吃掉那块区域的划词。根因是伴侣从磁盘加载回来的类名是基类 `PDFAnnotation`（不是 `PDFAnnotationPopup`），按类判一直漏摘 → 改按子类型判，并连带摘除父标注挂着的伴侣
- **标注点不中**（只能从右侧列表右键删除）：点选改走 PDFView 的 `mouseDown` 通道（PDFKit 自己的手势会吃掉标注上的点击），并把 PDF 自带的超链接/表单域/图形标注排除在点选之外——此前点目录跳转会冒出红虚线框 + 垃圾桶
- **每次标注动作后主线程卡 0.4~1.5 秒**（新建批注要等一会儿才能打字、点色不跟手、关框后划词/滚动不跟手）：主线程采样定位到大头是 `PDFDocument.dataRepresentation()`——PDFKit 写回会把每一页重新绘制一遍，其中约一半时间耗在嵌入字体按 glyph 名查找抛 C++ 异常的栈展开。改成主线程只把标注收成纯数据（毫秒级），后台串行队列自己开一份私有 PDF 副本、重放本应用标注后落盘（PDFKit 对象不能跨线程共享，故用主线程从不触碰的独立对象图）。写回同时做到：PDF 自带的超链接/表单域原样保留、反复写回不重复堆积、删除能落地；批注编辑期间的变更仍只标脏、交互结束统一写
- **使用批注即闪退**：选区包围盒为 `CGRect.null` 时页→视图变换会算出 NaN，AppKit 遇非法几何直接 trap → 面板定位加几何守卫；捏合比例加除零与有限性守卫
- **PDF 里点目录跳转失效、鼠标不变小手**：标注设只读后 PDFKit 不再接管超链接，改由自己识别 Link 并处理光标与跳转（页内目标 / GoTo / URL / 翻页命名动作）
- **用 × 关掉全部窗口后重开恢复的是更早的工作区**：窗口清单在关窗与状态变化时即时发布，退出前冻结
- **分栏后在右栏打开文件会把右栏收掉**：空组收起挪到标签就位之后执行（此前清场阶段就把刚分出来的空右组一并收起，文件落回左栏）

## [0.1.5] - 2026-08-09

### 新增

- **多窗口隔离**：从 Finder 打开与当前工作区无关的文件时，新开一个独立窗口（侧栏空态、只有该文件），原工作区窗口纹丝不动；在已有工作区的窗口里再打开别的工作区也会新开窗口——各窗口的文件树、标签、标注、AI 会话完全隔离，互不影响。目标工作区（或该文件）已经开着时聚焦已有窗口而非重复打开；菜单命令（保存/查找/缩放/导出/只读标注）作用于当前焦点窗口
- **重启恢复全部工作区窗口**：上次退出时开着的每个工作区各自恢复窗口与标签现场（逐工作区记忆沙盒授权）；全部关窗后退出则只恢复最后一个工作区
- **AI 会话线程跟文件走（TextMate 范式）**：对话按文件记忆并统一存放在 `~/Library/Application Support/MarkPDF/ai-sessions.json`——同一个文件无论从 Finder 直接打开、还是从任意层级的文件夹（工作区）里打开，都是同一份连续的对话，不再分叉；旧版存在工作区 `.markpdf/ai-sessions.json` 里的会话首次打开该工作区时自动并入（原文件改名 `ai-sessions.migrated.json` 归档）
- **外部打开冷启动现场**：双击/拖 Dock 唤起时不再恢复无关工作区现场（侧栏空态、标签栏只放外部文件），编辑/标注/翻译/AI 助手全部功能照常；工作区快照原样封存，手动打开照常恢复
- **右侧面板整栏收起**：工具栏最右新按钮一键收起/展开右侧面板（缩略图/大纲/AI 助手同栏），正文吃满剩余空间，展开回到之前宽度
- **右侧面板宽度自由调整**：面板与正文间的分界线悬停变左右箭头，幽灵线预览、松手定格（240–640pt，不逐帧重排）
- **AI 会话/最近打开/收藏跟随文件改名与移动**：文件树内改名、拖拽移动（含文件夹与撤销重做）后，该文件的 AI 聊天记录、最近打开、收藏自动指向新路径不断线；工作区根文件夹在 Finder 里改名后重启同样照常找回标签现场、最近打开、收藏与全部 AI 会话（书签解析发现路径不一致即整体迁移）

### 修复

- **冷启动双击文件多出工作区窗口**：系统「打开文件」事件在 SwiftUI 场景创建前即捕获（AppleEvent 通道），首窗不再误恢复无关工作区；空窗口就地承接文件不残留多余空窗；关闭与自带快照恢复重复的系统窗口恢复（会凭空多窗）；连续双击第二个文件只开该文件窗口，不再多出「未命名」窗口
- **打开已有文件/工作区总带「未命名」欢迎页**：打开真实文件时自动关掉从未编辑的欢迎草稿（动过字的草稿保留）；空白工作区/新建窗口仍有欢迎页
- **「设为工作区」询问可能静默记成「仅打开文件」**：启动早期弹窗被系统秒回，改等应用激活后再弹
- **md 阅读滚动经过公式/表格/图片时滚动条跳跃**：大尺寸渲染块按内容估算高度并缓存实测高度（视口回收重建不再「单行高→真实高」反复跳），图片加载完成主动重测行高，KaTeX 渲染结果缓存降低滚动期主线程占用
- **左栏折叠/展开拉伸压缩右栏**：右侧面板移出三栏分栏、改为独立兄弟面板——左右两栏由结构保证互不影响（「拨条守门」路线被系统动画后布局覆盖，根治换架构）；缩略图/大纲与 AI 助手最小宽统一 300pt，切换不再忽宽忽窄
- **最近打开出现别的工作区的文件**（点开报无权限）：记录与展示双闸按工作区归属过滤，历史误录条目读出即滤

## [0.1.4] - 2026-07-29
### 新增

- **输入区高度自由调整**：消息列表与输入区间的分界线本身即拖拽区（悬停变上下箭头），拖动以幽灵线预览、松手定格高度（56–400pt，过程零重排不抖动）
- **AI 回复公式原生排版（SwiftMath）**：块级与行内公式改由纯 Swift 的 CoreText 排版（TeX 数学字体，子集覆盖分数/根式/上下标/大型运算符/矩阵/对齐）——同步量取尺寸，行内短公式以 2x 位图内联进原生文本流（随文字换行、参与系统选区），超高/超宽公式自动独占一行展示；渲染源以 vendor 形式入库（离线可构建）
- **AI 回复 markdown 块级渲染**：管道表格（表头加粗 + 分隔线）、引用块（竖条 + 灰字 + 嵌套缩进）、分割线、# 标题、无序/有序列表（含嵌套与 Tab 缩进）

### 修复

- **聊天列表近底部滚动条跳变**（高公式行靠近底部才被 LazyVStack 物化，总长突然撑大）：≤30 条消息改全量 VStack——内容高度首帧即精确，滚动条全程匀速成比例
- **聊天面板公式渲染架构纠偏**：弃用「一个公式一个 WKWebView」（WebContent 进程开销线性放大、高度异步回填致滚动条抽搐、跨视图选区割裂——调研证实无主流产品采用该形态），整体迁移 SwiftMath，滚动稳定且成比例
- **流式生成时上翻历史被拽回底部**：滚动改贴底跟踪——仅视口贴底时自动滚到底，上翻查看历史不打扰，回到底部自动恢复；拖条期间不做任何程序化滚动（此前 1pt 锚点在可见边界反复触发「贴底/松手」致底部抽搐）
- **AI 助手输入框长文无法滚动**：TextField 换 TextEditor（超出自动内滚 56–120pt 自适应），⌘↵ 发送、回车换行；光标与占位提示对齐
- **AI 会话双键合并排序失效**（落盘时全量刷新 updatedAt，真实数据上按时间合并退化为随机序）：线程改记真实修改时间（仅消息变化时刷新），合并顺序与摘要归属随之正确；docKey 统一解析符号链接
- **连接测试补充集成回归**（空正文信封通过/形状错配拒绝）；解码结构错误文案统一为「响应格式无法识别」（不再显示英文 DecodingError 详情）
- **AI 回复 markdown 行分类边界**：Tab 缩进按一级计、前导空格标题正常渲染、空 `# ` 行不再渲染为空白标题
- **设置 → AI 模型行删除后可能串行/焦点错位**：模型规格增加稳定 id，行绑定与焦点键一律按 id；删除焦点行自动清焦点

## [0.1.3] - 2026-07-27
### 新增

- **上下文压缩重构与引用纪律（v1.4，对标 PaperQA2/Cline/Claude Code/Aider/LobeChat 源码）**：长对话压缩改「近期原文保留区 + 增量滚动」——最近轮次完整保留原文，只把滚出保留区的旧轮次并入摘要（不再压最旧一半）；摘要预算按压缩比自适应（长对话给更大摘要），摘要改六小节结构化（问题意图/文档锚点/结论/用户偏好/未决/当前进展，关键处逐字直引），漏掉的引用锚点由代码自动补全；回答引用更严格——只引本轮真实出现的锚点（附白名单）、句末引用、材料不足时明确说「无法从所给材料回答」并建议检索方向；AI 检索时每轮注入进度状态促尽早作答，工具使用纪律成文（已有上下文优先、失败换更宽关键词等）
- **AI 助手 Agent 化（v1.3）**：工作区检索交给模型自主决策——AI 按你的问题自行判断是否检索、用什么关键词，可连续「搜索 → 看大纲 → 读章节」再作答（最多 6 轮），过程以活动条实时展示（悬停看结果）；「工作区」开关 = 是否允许 AI 使用这些只读工具
- **模型窗口与回复长度自定义（v1.3）**：设置 → AI 里每个模型可配置上下文窗口（新增时按模型名预填建议值）；「回复长度上限」决定单次回答最大长度——文档/历史附带量按这两个值自动分配，不再猜测
- **对话上下文分层管理（v1.3）**：超长对话自动把早期轮次压缩为摘要（保留结论与引用锚点），近期原文 + 摘要一起送模型；摘要随会话保存，重启后长对话不失忆
- **侧边栏 AI 助手（FR-AI.2）**：右侧替代式单栏面板（工具栏按钮 / 命令面板 / ⌘⇧A 三入口，显隐随工作区记忆并重启恢复）；多轮流式对话可停止/重试；提问可附带编辑器或 PDF 选区与当前文档全文（面板内 chips 与设置同步，超长自动截断并注明）；回复五动作——插入光标 / 替换选区（可 ⌘Z）/ 复制 / 存为新笔记 / PDF 转带回链引用；轻量 markdown 渲染（代码块带复制）
- **AI 助手编排升级（调研驱动 v1.2）**：① 附带文档按所选模型上下文动态预算——大窗口模型整篇论文直接喂入，不再固定截 8000 字；② 超长文档自动「结构选节」——先把目录给模型挑相关章节再作答（md 标题树 / PDF 书签），问「结论」不再因头部截断而失败；③ 工作区检索首版（已被 v1.3 工具调用取代）；④ 回答按 [§章节]/[p.页码] 锚点引用来源
- **AI 会话持久保留**：对话按文档各自成线程（切文档自动切换、互不串扰），随工作区存入 `.markpdf/ai-sessions.json`，重启原样恢复；无工作区时内存态并注明
- **翻译与 AI 助手独立选型**：设置 → AI 可分别为划词翻译和助手对话选择「Provider × 模型」（翻译可用便宜小模型、助手用大模型）；一个 Provider 支持配置多个模型（逗号分隔），全部进入模型下拉框

- **界面语言设置（中文 / English / 跟随系统）**：设置 → 通用 → 语言。全部界面文案、菜单命令、系统菜单（About/Settings/Quit 等）与编辑器内核提示随语言切换；重启 App 后生效（切换时弹提示，可一键退出）。欢迎文档双语
- **Finder 直接打开 PDF / Markdown**：双击、「打开方式」或拖到 Dock 图标即可打开；所在文件夹非当前工作区时询问「设为工作区」——确认后弹已定位到该文件夹的系统授权面板，点一下即完成（沙盒授权 + 书签永久记忆，同文件夹之后静默）；选「仅打开文件」也能立即阅读编辑该文件（工作区相关功能降级：无文件树/图片粘贴/反链）
- **设为默认打开应用**：设置 → 通用 → 默认打开方式，Markdown 与 PDF 两个开关；开关反映系统真实状态（在 Finder 改走会显示关闭），macOS 无「取消默认」故关闭为无操作

### 修复

- **AI 助手面板字体偏小**：对话正文上调至 14pt（标题按级 14/15 加粗、代码块 13），「选中文字/当前文档/工作区」chips 与输入框同步加大
- **连接测试对 always-thinking 模型误报「响应格式无法识别」**（如 Kimi K3 思考耗尽小配额、正文为空属正常响应）：连接测试改信封校验——补全结构完整（choices/content 存在、无 error 载荷）即通过，不验正文非空
- **AI 会话同一文件裂成两条线程**（工作区打开按相对路径、外部双击按绝对路径）：线程 key 统一绝对路径，旧相对 key 载入时自动迁移、双线程按时间合并；另修迁移落盘时序竞态——启动「先绑文档、后载工作区」时激活线程被空消息覆盖丢失
- **AI 回复 markdown 列表/标题原文显示**（`- `/`## `）：段落按行块级渲染——无序/有序列表圆点/序号 + 悬挂缩进、标题按级分字重；消息动作条 hover 才出现导致移鼠即消失点不到，改常驻淡显、悬停变实
- **设置 → AI 模型列表行不对齐**：macOS Form 无标签自定义行必被顶到右缘，改 LabeledContent 原生两栏；名称框定宽对齐；名称/窗口框回车或点击空白处退出焦点（光标不再滞留闪烁）
- 打开 PDF 时右侧面板（缩略图/书签/标注/引用）默认占最宽：首次布局改为压到最窄（266pt），拖动调整后仍按系统记忆恢复；另每次启动都把右侧面板压回最窄（此前恢复上次列宽经常挡住正文）
- **每次 build 后启动弹「输入钥匙串密码」**（开发场景）：启动只查 Key 存在性不读明文；明文仅在实际发 AI 请求时读取
- **外部打开降级链路**：工作区子目录文件双击不再误弹「设为工作区」；裸开工作区外 md 粘贴图片不再静默写入无关工作区；裸开的文件标签不再污染工作区快照（重启不再弹「无权限」）；裸开 PDF 标注写回失败时引导设为工作区
- **界面语言**：补齐全功能英文译文（此前外部打开弹窗/设置节在英文界面显示中文）；导出 HTML 标题随语言；搜索面板占位符本地化；繁体系统「跟随系统」不再中英混杂

## [0.1.2] - 2026-07-24

### 变更

- **系统要求抬升至 macOS 15 Sequoia**（系统翻译 `TranslationSession` 依赖 macOS 15 API）；macOS 13/14 用户请停留在 0.1.1

### 新增

- **PDF 划词翻译（FR-AI.1）**：划词松手在浮动工具条下方弹出译文气泡；双引擎可切换——系统翻译（macOS 15 端侧模型）/ AI 大模型（OpenAI 兼容 + Anthropic，API Key 存 Keychain）；默认中英互译自动判断；顶部工具栏翻译开关（开 = 划词即翻，关 = 浮动条按钮手动翻）；设置 → AI 分区管理 Provider/模型/引擎/目标语言，首次使用弹隐私告知
- **标签页按工作区隔离**：每个工作区记住自己的标签组。切换工作区后不再看到上一个工作区的标签；切回时原样恢复（含分栏与激活状态）

### 修复

- **打完字立刻切标签/关窗/退出丢最后几个字符**：内核 300ms 上报防抖窗口内的输入现在会在页面隐藏/销毁前立即上报并落盘
- **标注后立刻切标签/关窗丢标注**：PDF 标注写回防抖窗口内的改动在切换/关闭/切换只读模式前强制落盘
- **撤销重命名/移动后编辑造成双文件数据分叉**：撤销链现在同步迁移标签，自动保存不再在旧文件名重建文件
- **关窗重开丢草稿**：窗口恢复只执行一次，不再整体替换内存中的标签组
- **文件从废纸篓放回后重开编辑永不保存**：重新打开已恢复的文件会正确重载并恢复自动保存
- 撤销「新建文件夹」重做出来的是空文件；仅改大小写的重命名（如 `Note.md`→`note.md`）被误报已存在
- 导出标注到读不出来的既有笔记（如 UTF-16 编码）会整体覆盖原文件：现在报错中止
- 反向链接漏报：文件名含空格/中文（%20 编码回链）、大小写变体、`<含空格>` 角标链接三种情况现在都能命中
- **分栏时同一文件可在左右栏双开、编辑互相覆盖**：现在全窗口去重，已打开的文件会切换到已有标签
- **分栏双 PDF 时 A 窗标注写进 B 文档**：标注 Store 现在跟随焦点（最近点击的窗格），切换前先落盘旧文档
- 分栏开着 PDF 时点回链/搜索结果，页码跳转可能跳到错误的文档：现在按目标文件匹配跳转
- **滚动 PDF 时右侧缩略图与工具栏页码延迟约 1 秒才跟随**：PDFKit 在连续滚动中不更新当前页；现在按视口中心实时跟页，缩略图滚动不停也实时高亮同步（原生缩略图组件换为自绘列表，点击跳页不变）
- **双栏论文选区跨栏误选**：右栏划词连带左栏（甚至大片三角选中区）——拖拽结束按起止点所在栏裁剪
- **划词翻译气泡**：译文停「翻译中」不显示（系统会话复用根治）、长译文无限撑高（240pt 上限+滚动）、滚动条白底突兀、正文显示小手光标（改 I 形）、点复制会闪烁重翻、定位换边跳变/大面积遮挡正文（新算法：鼠标侧优先+一次定边）、源文本断词与碎换行（送译前整理）
- `[^注](2024)` 这类脚注后接括号的写法会导致编辑器装饰重叠（渲染异常隐患）：现在整体按链接渲染
- 标签橙点、状态栏字数、工具栏模式选择器不实时刷新；打开标签时偶发「视图更新中发布」警告
- 切换 PDF 后查找状态与缩放可能串到下一篇文档；PDF 打不开时永久空白（现在有错误提示和重试按钮）
- sidecar 标注文件损坏时标注无声消失：现在明确提示，并在修复前暂停写回防止覆盖原文件
- 批注包含 emoji 时编辑光标落位错误；标注组改名后点击别处名字丢失（现在失焦自动提交）
- 移动/删除文件夹后，其中已打开文件的标签仍指向旧路径；文件改名换扩展名后标签类型不更新
- 从 Finder 拖入工作区外的文件无反应（现在明确拒绝）；一次拖入多个文件只处理第一个
- 红钮关窗后直接 ⌘Q，最后 0.5 秒内的编辑可能丢失（退出兜底现在无窗口也生效）
- md 查找栏 ↑↓ 到首尾命中后不再回绕；标签过多时标签栏无法滚动
- 排除目录名单扩充（Pods、.venv、dist、build 等），打开这类项目文件夹更不容易卡
- API Key 粘贴含换行/空格导致全线 401；baseURL 强制 https 防明文泄钥；隐私告知取消后不再反复弹窗

### 性能

- 自动保存不再引发全量目录重扫后的反链重读；`node_modules` 等重型目录内的文件变化不再触发任何刷新
- 字数统计改后台计算；Markdown 文件打开/保存移出主线程，长文档输入更顺滑
- 编辑器内核：纯移动光标/选择不再触发全文重扫（装饰扫描记忆化），大文档更流畅
- 标注列表改缓存读取，切换工具/色板不再全文档重扫；快速打开/命令面板消除重复匹配计算
- 图片预览改后台解码，大图打开不再卡住界面；全文搜索对超大 PDF 有上限且可随时取消
- PDF 缩略图缓存上限 + 换文档清空（大文档滚一遍不再驻留数百 MB 位图）

## [0.1.1] - 2026-07-22

### 新增

- **PDF 大文档异步解析加载**：解析移出主线程（237 页 ~1s 卡顿消除），代际令牌防串档，加载中显示进度指示
- **代码块重做**：边框排版、语言选择下拉（15 种语言）、一键复制按钮、注释正体（去伪斜体）
- **脚注双向跳转**：点击上标引用跳到定义（居中显示），定义行尾 ↩ 回跳首处引用
- **功能说明手册**：快捷键速查 / Markdown 语法 / PDF 标注 / 联动 / 设置 / 已知限制（见 `docs/功能说明手册.md`）
- 右侧面板「大纲 / 反向链接」分界线可拖动
- EditorStore 单测与性能测试套件（NFR 指标入库，CI 每次跑）

### 修复

- **标注写回降级路径数据丢失风险**:`replaceItemAt` 失败后的降级重写若再失败会丢全部标注，现保留救援文件；PDF 标注读写/写回失败改为用户可见提示（NFR-5，持续失败只报一次）
- **跨文档 ⌘Z 串档**：切换文档后按 ⌘Z 会把上一文档内容写回当前文档（换档即重置撤销栈）
- **选区高亮**：整行色块改为文本级贴合（失焦后选区仍显示，观感接近原生文本框）
- 脚注点击跳转无反应（WebKit 在 mousedown 默认行为里先落光标销毁 widget，改 mousedown 拦截）
- 打开/自动保存失败的提示此前只记日志，现弹窗提示

### 变更

- 已知限制更新：切换文件会重置编辑器撤销栈（由 ⌘Z 串档修复带来的正式口径）

## [0.1.0] - 2026-07-21

首个发布版本（M1~M4 全部里程碑完成）。

### 新增

- **工作区**：文件夹工作区（递归展示 md/PDF/图片）、文件树操作（新建/重命名/删除/拖拽/撤销）、FSEvents 自动刷新、标签页 + 左右分栏、收藏夹与最近打开（10 条）、工作区状态持久化（重启恢复现场）
- **Markdown 编辑**：所见即所得/源码/阅读三模式、GFM 全集、KaTeX 公式、脚注、`==高亮==`、大纲 TOC、自动保存（500ms 防抖原子写）、图片粘贴/拖拽入 assets 并自动修正相对链接、字数统计、导出 PDF（A4 分页）/HTML、打字机模式与专注模式、⌘F 文档内查找
- **PDF 阅读**：连续滚动/单页/双页、缩放 50%–400%、大纲/书签/缩略图、页内搜索、阅读位置记忆、夜间阅读主题（智能反色、图片不反色）
- **PDF 标注**：高亮/下划线/删除线、批注（页边标记 + 虚线连接器）、四色系统、标注列表面板、写回 PDF + .bak 备份、只读 sidecar 模式、导出全部标注为 Markdown（页码回链、增量去重）
- **联动**：复制为带回链的引用块（⇧⌘C）、md 中 PDF 回链跳转并闪烁、反向链接面板
- **搜索导航**：⌘P 文件名模糊搜索、⌘⇧F 全文搜索（md + PDF）、⌘O 命令面板（拼音首字母匹配）

### 已知限制

- 未签名分发：首次打开需右键 →「打开」（未购买 Developer ID）
- 标签切换会重置编辑器撤销栈；侧栏宽度与分栏比例不持久（macOS 13 API 限制）；草稿内容不持久
- 阅读主题为图层滤镜方案（非内容流级反色），极端彩色图可能轻微失真
- 分享出去的 HTML 中 KaTeX 样式依赖本机 bundle 路径（他机公式样式丢失）

### 移除（经决策）

- 便签标注（FR-4.2）、波浪线（PDFKit 不支持）、手绘/矩形/箭头、mermaid、羊皮纸主题
