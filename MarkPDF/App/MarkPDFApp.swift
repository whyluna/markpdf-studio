import SwiftUI

@main
struct MarkPDFApp: App {
  var body: some Scene {
    WindowGroup {
      ContentView()
        .frame(minWidth: 1080, minHeight: 640)
    }
    .defaultSize(width: 1380, height: 900)
  }
}
